---
import Layout from '../../layouts/Layout.astro';
import content from '../../content/formation.json';
---

<Layout title="Formation IA — De zéro à l'autonomie">

  <section class="section-hero">
    <div class="wrap">
      <span class="badge mb-6">
        {content.hero.badge}
      </span>
      <h1 class="titre-page">
        {content.hero.title}<br>
        {content.hero.subtitle}
      </h1>
      <p class="chapo mt-4">
        {content.hero.description}
      </p>
      <div class="flex flex-wrap gap-3 mt-8">
        <a href="#assistant-financement" class="btn btn-or">
          {content.hero.cta1}
        </a>
        <a href="#offres" class="btn btn-ligne">
          {content.hero.cta2}
        </a>
      </div>
      <ul class="flex flex-wrap gap-3 mt-8 texte-secondaire">
        {content.hero.badges.map(b => <li class="bg-white border border-sauge-clair rounded-full px-3 py-1.5">{b}</li>)}
      </ul>
      <blockquote class="mt-12 text-lg text-texte/70 italic border-l-4 border-sauge pl-4">
        {content.hero.temoignage}
      </blockquote>
    </div>
  </section>

  <section class="section bg-sauge-clair/50">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.probleme.titre}</h2>
      <p class="text-texte/80 mesure">{content.probleme.texte1}</p>
      <p class="text-texte/70 mt-4 mesure">{content.probleme.texte2}</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-8">
        <div class="bg-white border-l-4 border-sauge rounded-r-lg px-5 py-4">
          <span class="titre-section text-sauge-fonce">{content.probleme.stat1_valeur}</span>
          <p class="texte-secondaire">{content.probleme.stat1_texte}</p>
        </div>
        <div class="bg-white border-l-4 border-sauge rounded-r-lg px-5 py-4">
          <span class="titre-section text-sauge-fonce">{content.probleme.stat2_valeur}</span>
          <p class="texte-secondaire">{content.probleme.stat2_texte}</p>
        </div>
      </div>
      <p class="mesure mt-8 text-texte/70 font-medium">{content.probleme.chute}</p>
    </div>
  </section>

  <span id="assistant-financement" class="block h-0 scroll-mt-24"></span>
  <section id="financement" class="section">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.financement.titre}</h2>
      <div class="text-texte/80 space-y-4">
        <p>{content.financement.intro}</p>
      </div>

      {content.financement.statuts.map(s => (
        <div class="bg-white border-l-4 border-sauge rounded-r-xl p-5 mt-6">
          <h3 class="titre-carte">{s.titre}</h3>
          <p class="texte-secondaire mt-1">{s.texte}</p>
          {s.citation && <blockquote class="legende italic mt-2 bg-sauge-clair rounded-lg px-3 py-2">{s.citation}</blockquote>}
        </div>
      ))}

      <div class="bg-anthracite text-white/80 rounded-xl p-6 mt-6 border-t-4 border-dore">
        <h3 class="titre-carte text-white">{content.financement.cpf.titre}</h3>
        <p class="text-sm mt-2">{content.financement.cpf.texte}</p>
        <a href="/certification/" class="btn btn-or mt-4">
          {content.financement.cpf.cta}
        </a>
      </div>

      <div class="bg-white border border-dashed border-sauge rounded-xl p-5 mt-4">
        <h3 class="titre-carte">{content.financement.reserve.titre}</h3>
        <p class="texte-secondaire mt-1">{content.financement.reserve.texte}</p>
        <a href="#assistant-financement" class="btn btn-or mt-3">
          {content.financement.reserve.cta}
        </a>
      </div>
    </div>
  </section>

  <section class="section bg-sauge-clair/50">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.promesse.titre}</h2>
      <p class="text-texte/80 mesure">{content.promesse.texte}</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-8">
        {content.promesse.cards.map(c => (
          <div class="bg-white border border-sauge-clair rounded-xl p-5">
            <span class="block w-8 h-0.5 bg-dore mb-3"></span>
            <h3 class="titre-carte">{c.titre}</h3>
            <p class="texte-secondaire mt-1">{c.texte}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="section">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.format.titre}</h2>
      <ul class="mesure space-y-2 text-texte/80">
        {content.format.items.map(item => (
          <li class="flex gap-2"><span class="text-sauge-fonce">→</span> {item}</li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section bg-anthracite">
    <div class="wrap">
      <h2 class="titre-section text-white mb-4">{content.formateur.titre}</h2>
      <div class="mesure text-white/70 space-y-3">
        {content.formateur.paragraphes.map(p => <p>{p}</p>)}
      </div>
      <p class="mt-4 text-sm">
        <a href="mailto:hermitant.ecolibre@gmail.com" class="text-dore hover:underline">{content.formateur.email}</a>
        · <a href="tel:+33646246199" class="text-dore hover:underline">{content.formateur.tel}</a>
      </p>
    </div>
  </section>

  <section class="section bg-anthracite">
    <div class="wrap">
      <h2 class="titre-section text-white mb-4">{content.preuve.titre}</h2>
      <div class="mesure text-white/70 space-y-4">
        {content.preuve.paragraphes.map((p, i) => (
          <p>{p}</p>
        ))}
      </div>
    </div>
  </section>

  <span id="offres" class="block h-0 scroll-mt-24"></span>
  <section class="section">
    <div class="wrap">
      <h2 class="titre-section mb-8">{content.offres.titre}</h2>

      <div class="bg-white border border-sauge-clair rounded-xl p-6 mb-4 border-l-4 border-l-dore">
        <div class="flex flex-wrap items-baseline gap-3 mb-3">
          <h3 class="titre-carte">{content.offres.initiation.titre}</h3>
          <span class="text-xs font-bold uppercase bg-dore text-anthracite rounded-full px-2.5 py-1">{content.offres.initiation.badge}</span>
        </div>
        <p class="texte-secondaire">{content.offres.initiation.texte}</p>
        <p class="texte-secondaire mt-3">{content.offres.initiation.infos}</p>
        <a href="#contact" class="btn btn-or mt-3">
          {content.offres.initiation.cta}
        </a>
      </div>

      <div class="bg-white border border-sauge-clair rounded-xl p-6 mb-4">
        <div class="flex flex-wrap items-baseline gap-3 mb-3">
          <h3 class="titre-carte">{content.offres.n1n2.titre}</h3>
          <span class="titre-carte text-sauge-fonce">{content.offres.n1n2.prix}</span>
        </div>
        <p class="texte-secondaire">{content.offres.n1n2.texte}</p>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-4">
          <div class="bg-sauge-clair/30 rounded-lg p-4">
            <h4 class="titre-mini">{content.offres.n1n2.n1.titre}</h4>
            <p class="legende mt-1">{content.offres.n1n2.n1.texte}</p>
          </div>
          <div class="bg-sauge-clair/30 rounded-lg p-4">
            <h4 class="titre-mini">{content.offres.n1n2.n2.titre}</h4>
            <p class="legende mt-1">{content.offres.n1n2.n2.texte}</p>
          </div>
        </div>
        <p class="texte-secondaire mt-3">{content.offres.n1n2.infos}</p>
        <a href="#contact" class="btn btn-or mt-3">
          {content.offres.n1n2.cta}
        </a>
      </div>

      <div class="bg-white border border-sauge-clair rounded-xl p-6 mb-4">
        <div class="flex flex-wrap items-baseline gap-3 mb-3">
          <h3 class="titre-carte">{content.offres.n3n4.titre}</h3>
          <span class="titre-carte text-sauge-fonce">{content.offres.n3n4.prix}</span>
        </div>
        <p class="texte-secondaire">{content.offres.n3n4.texte} <span class="bg-dore/20 text-dore font-bold text-xs uppercase rounded-full px-2 py-0.5">{content.offres.n3n4.badge}</span></p>
        <p class="texte-secondaire mt-3">{content.offres.n3n4.infos}</p>
        <a href="#contact" class="btn btn-ligne mt-3">
          {content.offres.n3n4.cta}
        </a>
      </div>

      <div class="bg-white border border-sauge-clair rounded-xl p-6 mb-4">
        <div class="flex flex-wrap items-baseline gap-3 mb-3">
          <h3 class="titre-carte">{content.offres.n5.titre}</h3>
          <span class="titre-carte text-sauge-fonce">{content.offres.n5.prix}</span>
        </div>
        <p class="texte-secondaire">{content.offres.n5.texte}</p>
        <p class="texte-secondaire mt-3">{content.offres.n5.infos}</p>
        <a href="#contact" class="btn btn-ligne mt-3">
          {content.offres.n5.cta}
        </a>
      </div>

      <div class="bg-white border border-sauge-clair rounded-xl p-6 mb-4">
        <div class="flex flex-wrap items-baseline gap-3 mb-3">
          <h3 class="titre-carte">{content.offres.certification.titre}</h3>
          <span class="text-xs font-bold uppercase bg-sauge-clair text-sauge-fonce rounded-full px-2.5 py-1">{content.offres.certification.badge}</span>
        </div>
        <p class="texte-secondaire">{content.offres.certification.texte}</p>
        <a href="/certification/" class="btn btn-ligne mt-3">
          {content.offres.certification.cta}
        </a>
      </div>

      <div class="bg-white border border-sauge-clair rounded-xl p-6">
        <h3 class="titre-carte">{content.offres.themes.titre}</h3>
        <span class="titre-carte text-sauge-fonce">{content.offres.themes.prix}</span>
        <p class="texte-secondaire mt-2">{content.offres.themes.texte}</p>
        <a href="#contact" class="btn btn-ligne mt-3">
          {content.offres.themes.cta}
        </a>
      </div>
    </div>
  </section>

  <section class="section bg-sauge-clair/50">
    <div class="wrap">
      <h2 class="titre-section mb-6">{content.intraInter.titre}</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="bg-white border border-sauge-clair rounded-xl p-6">
          <h3 class="titre-carte">{content.intraInter.intra.titre}</h3>
          <p class="texte-secondaire mt-2">{content.intraInter.intra.texte}</p>
        </div>
        <div class="bg-white border border-sauge-clair rounded-xl p-6">
          <h3 class="titre-carte">{content.intraInter.inter.titre}</h3>
          <p class="texte-secondaire mt-2">{content.intraInter.inter.texte}</p>
        </div>
      </div>

      <div class="bg-white border border-sauge rounded-xl p-6 mt-4">
        <h3 class="titre-carte">{content.intraInter.calcul.titre}</h3>
        <p class="texte-secondaire mt-2">{content.intraInter.calcul.texte}</p>
        <div class="overflow-x-auto mt-4">
          <table class="w-full text-sm">
            <thead>
              <tr class="text-xs uppercase text-sauge-fonce">
                <th class="text-left py-2 pr-4"></th>
                <th class="text-left py-2 pr-4">Inter (Ardèche)</th>
                <th class="text-left py-2">Intra (chez vous)</th>
              </tr>
            </thead>
            <tbody class="text-texte/70">
              {content.intraInter.calcul.lignes.map(l => (
                <tr class="border-t border-sauge-clair">
                  <td class="py-2 pr-4">{l[0]}</td>
                  <td class="py-2 pr-4">{l[1]}</td>
                  <td class="py-2"><strong>{l[2]}</strong></td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <p class="legende mt-3">{content.intraInter.calcul.note}</p>
        <div class="flex flex-wrap gap-3 mt-4">
          <a href="#contact" class="btn btn-or">
            {content.intraInter.cta1}
          </a>
          <a href="#contact" class="btn btn-ligne">
            {content.intraInter.cta2}
          </a>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.lieu.titre}</h2>
      <p class="mesure text-texte/80">{content.lieu.texte}</p>
      <a href="#contact" class="btn btn-or mt-4">
        {content.lieu.cta}
      </a>
    </div>
  </section>

  <section class="section bg-sauge-clair/50">
    <div class="wrap">
      <h2 class="titre-section mb-6">{content.pourQui.titre}</h2>
      <ul class="mesure space-y-3">
        {content.pourQui.items.map(item => (
          <li class="flex gap-3 text-texte/80"><span class="text-sauge mt-1">●</span> {item}</li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section">
    <div class="wrap">
      <h2 class="titre-section mb-6">Questions fréquentes</h2>
      <div class="border-t border-sauge-clair">
        {content.faq.map((item, i) => (
          <details class="border-b border-sauge-clair py-4" {i === 0 ? 'open' : ''}>
            <summary class="cursor-pointer list-none titre-carte flex justify-between items-center">
              {item.question}
              <span class="text-sauge text-xl">{i === 0 ? '−' : '+'}</span>
            </summary>
            <p class="texte-secondaire mt-2">{item.reponse}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <section class="section bg-sauge-clair/50 text-center">
    <div class="wrap">
      <h2 class="titre-section mb-4">{content.ctaFinal.titre}</h2>
      <p class="text-texte/70 mb-6">{content.ctaFinal.sousTitre}</p>
      <a href="#contact" class="btn btn-or">
        {content.ctaFinal.cta}
      </a>
      <p class="legende mt-4">{content.ctaFinal.mentions}</p>
    </div>
  </section>

  <span id="contact" class="block h-0 scroll-mt-24"></span>
</Layout>
